Prerequisite
- Node must be installed on the machine


Steps to run the application

1. Run command "npm install"
2. Set "PORT" env variable for some port number on which the application should run, else by default it will run on 3000.
2. Run command "npm start"